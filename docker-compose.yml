version: '3.1'

services:

  postgres:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: local
    ports:
      - 5432:5432
  server:
    build: 
      context: .
      dockerfile: dockerfile.server
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
    ports:
      - 5000:80
  worker:
    build: 
      context: .
      dockerfile: dockerfile.worker
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - DB_PASSWORD=gpkdcRIEyiZVQLugeiJI
    depends_on:
      - postgres
      - server
